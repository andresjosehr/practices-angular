import { CounterBtnIncrementDirective } from './counter-btn-increment.directive';

describe('CounterBtnIncrementDirective', () => {
  it('should create an instance', () => {
    const directive = new CounterBtnIncrementDirective();
    expect(directive).toBeTruthy();
  });
});
