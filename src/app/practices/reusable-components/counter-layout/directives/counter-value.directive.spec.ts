import { CounterValueDirective } from './counter-value.directive';

describe('CounterValueDirective', () => {
  it('should create an instance', () => {
    const directive = new CounterValueDirective();
    expect(directive).toBeTruthy();
  });
});
