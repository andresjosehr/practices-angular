import { CounterBtnDecrementDirective } from './counter-btn-decrement.directive';

describe('CounterBtnDecrementDirective', () => {
  it('should create an instance', () => {
    const directive = new CounterBtnDecrementDirective();
    expect(directive).toBeTruthy();
  });
});
